spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/requestVacation?serverTimezone=UTC&charaterEncoding=UTF-8
    username: root
    password: vincentj3044!

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org.hibernate.SQL: debug

